name: webserver
description: Restful API to serve Studentifier's requests
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.2 <3.0.0'

dev_dependencies:
  build_runner:
  build_web_compilers:

dependencies:
  test: ^1.16.0
  args: ^2.0.0
  shelf: ^1.1.0
  supabase: ^1.1.0
  intl: ^0.17.0